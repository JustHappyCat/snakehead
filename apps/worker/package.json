{
  "name": "@seo-spider/worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node --transpile-only src/index.ts",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "start": "node dist/index.js",
    "test": "ts-node --transpile-only src/test-utils.ts",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "@seo-spider/shared": "^1.0.0",
    "axios": "^1.6.0",
    "bullmq": "^4.0.0",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^17.2.4",
    "ioredis": "^5.0.0",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "prisma": "^5.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
